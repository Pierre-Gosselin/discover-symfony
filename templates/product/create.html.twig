{% extends 'base.html.twig' %}

{% block body %}


    {{ form_start(form) }}
        {{ form_widget(form)}}
        <button class="btn btn-primary">Ajouter le produit</button>
    {{ form_end(form) }}

{% endblock %}